extends: default

rules:
  document-start: disable

  line-length:
    max: 120
    level: error

  # accept both     key:
  #                   - item
  #
  # and             key:
  #                 - item
  indentation:
    indent-sequences: whatever
